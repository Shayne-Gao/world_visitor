# [OPEN] startup-current-location-ux

## Symptom
- 打开 APP 时，地图不会直接定位到当前用户位置。
- 蓝点首屏不稳定，经常需要手动点一次定位才出现。
- 自动记录状态、蓝点位置、地图视角三者体验割裂，不符合用户对“打开即在当前位置”的基本预期。

## Hypotheses
- H1: 启动时页面优先沿用旧缓存视角，但没有在首屏阶段把“当前实时位置”提升为更高优先级，因此地图仍停在历史位置。
- H2: 原生状态轮询拿不到有效 `lastLat/lastLng` 时，没有稳定触发一次首屏静默定位，导致蓝点和地图中心都缺失。
- H3: 首屏定位逻辑虽然触发了，但只更新蓝点，没有同步 `map.setView` / `flyTo` 到当前位置，因此用户体感仍是“停在奇怪的地方”。
- H4: 自动记录状态 UI 与地图显示链路分离，出现“显示在记录，但地图视角和蓝点没有进入当前态”的割裂体验。
- H5: 启动快开缓存路径与实时位置链路冲突，快开优化保住了缓存首屏，但牺牲了“立即落到当前位置”的基础体验。

## Product Bar
- 基础问题必须保证：
  - 打开 APP 后，优先让用户看到当前所在位置，而不是历史缓存视角。
  - 蓝点应尽快出现，不应依赖手动点一次定位。
  - 自动记录状态、蓝点、地图视角必须一致，不允许 UI 说在记录、地图却停在旧地方。

## Next Step
- 先核对启动定位、蓝点更新、地图中心切换和快开缓存路径的优先级关系。
- 先补足证据和链路日志，再做最小修复。

## Findings
- 页面已有 `ensureStartupLocationMarker()`，但现状只能保证“尽量去拿一个蓝点”。
- `refreshNativeTrackingStatus()` 在拿到原生 `lastLat/lastLng` 后只调用 `updateCurrentLocationMarker()`，不会同步把地图视角切到当前位置。
- `locateMe({ auto: true })` 虽然会尝试在首屏定位时设置地图中心，但它只覆盖“页面 geolocation 成功返回”的路径，不能覆盖“原生状态先恢复出最近位置”的路径。
- 这导致打开 APP 时经常出现：蓝点可能更新了，但地图仍然停在默认或历史位置，用户体感就是“没有直接定位到当前的位置”。

## Fix
- 新增统一的 `focusMapOnCurrentLocation()`，把“首屏当前位置聚焦”从零散分支收口成一个显式入口。
- 无论当前位置来自启动静默定位，还是来自原生状态里的最近位置，只要是首个有效当前坐标，就优先把地图中心切到那里。
