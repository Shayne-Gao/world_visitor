# [OPEN] debug-auto-tracking-broken

## Symptoms
- 打开 APP 后没有可靠地自动跟踪。
- 定位点不实时刷新，走远后仍停留在原处。
- 自动跟踪没有稳定写入轨迹，也没有持续点亮地图。

## Hypotheses
1. 原生前台跟踪服务实际上没有稳定启动，或启动后很快停止，导致既没有持续定位也没有持续写库。
2. 原生服务在持续拿定位，但没有把新点稳定 checkpoint 成 track segment，因此数据库里没有新增轨迹。
3. 原生数据库已有新增轨迹，但页面没有实时订阅/拉取增量，所以定位点和地图都没有更新。
4. “定位按钮”的一次性网页定位和“原生自动跟踪”的持续定位是两套链路，当前 UI 只刷新了网页定位点，没刷新原生实时位置。
5. 自动跟踪的时间间隔/位移阈值与“原地也要记录当前位置”的预期不一致，导致用户体感为“根本没在记录”。

## Current evidence
- 用户反馈：打开 APP 后自动跟踪基本不可用，定位点不刷新，地图不点亮。
- 历史日志显示：当前页面更多依赖原生 archive 增量同步，而不是实时位置流。
- 当前实现里“定位按钮”和“原生后台记录”确实是两套不同路径。

## Next step
- 先检查自动跟踪从“启动服务 -> 收到定位 -> 写 draft/track -> 推送页面实时位置/增量轨迹”的整条链路。
- 第一处代码修改仅增加打点，不改业务逻辑。
